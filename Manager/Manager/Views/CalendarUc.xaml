<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             x:Class="Manager.Views.CalendarUc"
             xmlns:viewModels="clr-namespace:Manager.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:CalendarUcVm}">
    <ContentPage.Content>
        <StackLayout x:Name="Background" Margin="10,10,10,10">
            <Picker ItemsSource="{Binding MonthSelect}" SelectedIndexChanged="IndexChanged" SelectedIndex="{Binding SelectedIndex}">
            </Picker>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>